export enum EventStatus {
    Pending = 'Pending',
    Accepted = 'Accepted',
    Declined = 'Declined',
  }
  
  export enum EventType {
    RemoteWork = 'RemoteWork',
    PaidLeave = 'PaidLeave',
  }
  